{
  "name": "root",
  "version": "0.0.0",
  "description": "Framework agnostic organism-level UI kit. A collection of components and utilities to build web applications with ease.",
  "homepage": "https://github.com/nazahex/kazaura",
  "license": "Apache-2.0",
  "private": true,
  "author": {
    "name": "KazViz",
    "email": "kaz@kazviz.com",
    "url": "https://kazviz.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nazahex/kazaura.git"
  },
  "bugs": {
    "url": "https://github.com/nazahex/kazaura/issues"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "test": "turbo run test --continue",
    "test:coverage": "turbo run test --continue -- --coverage",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome check --write && bun run prettier:write",
    "format:check": "biome check && bun run prettier:check",
    "ci:local": "bun run clean && bun run build && bun run test && bun run lint && bun run format:check",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "prettier:write": "prettier --write \"**/*.{md,yml,yaml}\"",
    "prettier:check": "prettier --check \"**/*.{md,yml,yaml}\"",
    "postinstall": "lefthook install"
  },
  "workspaces": [
    "components/*",
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.3.6",
    "@changesets/cli": "^2.29.7",
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@types/bun": "^1.3.2",
    "@types/node": "^24.10.1",
    "lefthook": "^2.0.4",
    "packlet": "^0.3.2",
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=18"
  },
  "packageManager": "bun@1.3.2"
}
